supergraph:
  listen: 127.0.0.1:4000
  introspection: true

sandbox:
  enabled: true

homepage:
  enabled: false

include_subgraph_errors:
  all: true

cors:
  allow_any_origin: true

health_check:
  listen: 127.0.0.1:8088

